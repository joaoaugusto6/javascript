/*
Opreradores de comparação
> maior que
>= maior ou igual a
< menor que
<= menor ou igual a
== igualdade (valor) - não recomendado
=== igualdade estrita (valor e tipo)
!= diferente (valor) - não recomendado
!== diferente estrito (valor e tipo)
*/

const num1 = 10;
const num2 = 20;
let comp1 = num1 > num2; // > maior que
console.log(comp);

const comp2 = num1 >= num2; // >= maior ou igual a
console.log(comp);

const comp3 = num1 < num2; // < menor que
console.log(comp);

const comp4 = num1 <= num2; // <= menor ou igual a
console.log(comp);